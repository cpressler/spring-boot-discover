# spring-boot-discover



## Docker

Interactive mode
```bash
docker run -i -t -p 8761:8761 --name eureka softvisionlvcp/spring-boot-discovery-server:latest
```

Dettached  mode
```bash
docker run -d -p 8761:8761 --name eureka softvisionlvcp/spring-boot-discovery-server:latest
```
